module Model.Groups (
    Address,
    Bill,
    Car,
    CarContact,
    CardNumber,
    Contact,
    CountedCost,
    Partner,
    Payment,
    Times,
    VinCar,
    VinContact
    ) where

import Model.Groups.Address (Address)
import Model.Groups.Bill (Bill)
import Model.Groups.Car (Car)
import Model.Groups.CarContact (CarContact)
import Model.Groups.CardNumber (CardNumber)
import Model.Groups.Contact (Contact)
import Model.Groups.CountedCost (CountedCost)
import Model.Groups.Partner (Partner)
import Model.Groups.Payment (Payment)
import Model.Groups.Times (Times)
import Model.Groups.VinCar (VinCar)
import Model.Groups.VinContact (VinContact)
